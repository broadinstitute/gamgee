language: cpp
compiler: gcc 
before_script:
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get -qq update
  - sudo apt-get -qq install g++-4.8 valgrind 
  - export CXX="g++-4.8"
  - git submodule update --init --recursive
  - make travis
script:
  - boost-build/bin/b2 -a
